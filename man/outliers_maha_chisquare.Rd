% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Intra-Ramanome-Analysis.R
\name{outliers_maha_chisquare}
\alias{outliers_maha_chisquare}
\title{Detect Outliers Using Mahalanobis Distance and Chi-squared Test}
\usage{
outliers_maha_chisquare(data, p = 0.1)
}
\arguments{
\item{data}{A numeric matrix or data frame.}

\item{p}{Threshold probability (default is 0.1).}
}
\value{
A vector containing the indices of the detected outliers.
}
\description{
This function detects outliers in a dataset using the Mahalanobis distance and chi-squared test.
Outliers are defined as observations with Mahalanobis distances that have chi-squared test p-values below a certain threshold.
}
\examples{
# Create sample data with outliers
set.seed(123)
normal_data <- matrix(rnorm(100), nrow=20)
outlier_data <- matrix(rnorm(10, mean=5), nrow=2)
data <- rbind(normal_data, outlier_data)

}
